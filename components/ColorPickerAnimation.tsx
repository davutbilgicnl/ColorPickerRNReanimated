import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const ColorPickerAnimation = () => {
  return (
    <View>
      <Text>ColorPickerAnimation</Text>
    </View>
  );
};

export default ColorPickerAnimation;

const styles = StyleSheet.create({});
